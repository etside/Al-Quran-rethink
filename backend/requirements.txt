# Pure-Python pins: pydantic-core has no wheels for Python 3.14 / Android
# (PRoot) environments; pydantic 1.x + FastAPI 0.95 keep the whole stack
# build-free while staying FastAPI. Docker images use the same pins.
fastapi==0.95.2
pydantic>=1.10.19,<2
uvicorn>=0.30
sqlalchemy>=2.0
httpx>=0.27
